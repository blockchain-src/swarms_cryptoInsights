# 基础库
requests==2.28.1          # 用于发送 HTTP 请求
beautifulsoup4==4.11.1    # 用于解析 HTML 页面

# 数据分析和可视化
matplotlib==3.6.0         # 用于生成趋势图
textblob==0.17.1          # 用于情绪分析

# 环境变量管理
python-dotenv==1.0.0      # 用于加载 .env 文件中的环境变量

# Swarms 框架（如使用，请确保正确版本）
swarms==1.0.0             # 假设的框架版本，需确认实际版本

# 补充依赖（如果需要）
# 如果有其他 NLP 或 API 调用需求，可以在此添加：
# pandas==1.5.0           # 可选：用于数据处理
# tweepy==4.10.1          # 可选：用于访问 Twitter API
